# Share-stability analysis with stability filter enabled.

analysis_type: share_stability

years:
  start: 2004
  end: 2008
  target: 2006

break:
  period: "20062007"
  direction: "b_to_a"

measures:
  weights_source: VALUE_EUR
  analysis_measure: VALUE_EUR

metrics: ["r2_45", "exposure_weighted", "diffuseness_weighted"]

paths:
  concordance_path: data/concordances/CN_concordances_1988_2025_XLS_FORMAT.xls
  concordance_sheet: null
  annual_base_dir: data/extracted_annual_no_confidential/products_like
  weights_dir: outputs/weights/adjacent
  output_dir: outputs/analysis/share_stability_filtered

chaining:
  finalize_weights: true
  neg_tol: 0.000001
  pos_tol: 0.0000000001
  row_sum_tol: 0.000001

sample:
  exclude_reporters: []
  exclude_partners: []

stability_filter:
  enabled: true
  years: [2004, 2005, 2007]

plot:
  output_path: outputs/analysis/share_stability_filtered/share_stability.png
  #title: "CN8 trade-share stability: HS2007 non-trivial components"
  point_alpha: 0.3
  point_size: 8
  axis_padding: 0.04
  point_color: "black"
  use_latex: true
  latex_preamble: "\\usepackage{newtxtext,newtxmath}"
